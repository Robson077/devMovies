import styled from "styled-components"

export const Container = styled.div`
    background: #000000;
    padding: 0 20px;
    overflow: hidden;

    h2 {
        color: #ffffff;
        font-size: 25px;
        margin: 50px 0 20px 20px;
    }

    .swiper-wrapper {
        display: flex;
        flex-direction: row;
    }
`
